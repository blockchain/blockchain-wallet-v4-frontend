import { path } from 'ramda'

export const getAdverts = path(['misc', 'adverts'])

export const getCaptcha = path(['misc', 'captcha'])

export const getPriceIndexSeries = path(['misc', 'charts', 'price_index_series'])

export const getLogs = path(['misc', 'logs'])

export const getTransactionHistory = path(['misc', 'reports', 'transactions'])
