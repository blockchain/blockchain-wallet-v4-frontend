import * as AT from './actionTypes'

export const refreshBtcTransactions = () => ({ type: AT.REFRESH_BTC_TRANSACTIONS })
export const refreshClicked = () => ({ type: AT.REFRESH_CLICKED })
export const refreshRates = () => ({ type: AT.REFRESH_RATES })
