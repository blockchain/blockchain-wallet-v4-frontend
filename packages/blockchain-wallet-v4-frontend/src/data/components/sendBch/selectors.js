import { path } from 'ramda'

export const getStep = path(['components', 'sendBch', 'step'])

export const getPayment = path(['components', 'sendBch', 'payment'])

export const getToToggled = path(['components', 'sendBch', 'toToggled'])

