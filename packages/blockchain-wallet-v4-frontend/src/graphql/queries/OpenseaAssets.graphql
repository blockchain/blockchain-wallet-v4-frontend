query OpenseaAssets($collectionSlug: String, $limit: [Int], $offset: [Int]) {
  openseaAssets(collection_slug: $collectionSlug, limit: $limit, offset: $offset) {
    name
    token_id
    contract_address
    owner_address
    image_original_url
    permalink
  }
}