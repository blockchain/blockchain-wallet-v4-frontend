query Assets($filter: [AssetFilter], $sort: AssetSort, $forSale: Boolean, $limit: Int, $offset: Int) {
  assets(filter: $filter, sort: $sort, forSale: $forSale, limit: $limit, offset: $offset) {
    name
    token_id
    contract {
      address
    }
    image_url
    permalink
    owners {
      address
    }
    collection {
      name
      image_url
    }
  }
}
