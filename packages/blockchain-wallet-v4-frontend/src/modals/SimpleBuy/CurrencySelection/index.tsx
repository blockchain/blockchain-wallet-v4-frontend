import { reduxForm } from 'redux-form'
import React from 'react'

type Props = {}

const CurrencySelection: React.FC<Props> = () => {
  return <div />
}

export default reduxForm({ form: 'sbCurrencySelection' })(CurrencySelection)
