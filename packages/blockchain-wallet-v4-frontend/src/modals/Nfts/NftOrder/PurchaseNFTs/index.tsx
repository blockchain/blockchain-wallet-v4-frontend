import React from 'react'

import { Props as OwnProps } from '..'

const PurchaseNFTs: React.FC<Props> = (props) => {
  return <>Purchase NFTs</>
}

type Props = OwnProps

export default PurchaseNFTs
