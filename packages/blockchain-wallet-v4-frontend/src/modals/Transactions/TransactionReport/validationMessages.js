import { FormattedMessage } from 'react-intl'
import React from 'react'

export const InvalidDateMessage = () => (
  <FormattedMessage
    id='modals.transactionreport.invalid'
    defaultMessage='Invalid date'
  />
)
