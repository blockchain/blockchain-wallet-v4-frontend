import { FC } from 'react'

export type VerticalDashedLineProps = {
  bottom: number
  color: string
  left: number
  top: number
}

export type VerticalDashedLineComponent = FC<VerticalDashedLineProps>
