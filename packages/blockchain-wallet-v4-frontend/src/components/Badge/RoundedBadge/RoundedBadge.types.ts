import { Colors } from '@blockchain-com/constellation'

export type RoundedBadgeProps = {
  bgColor?: Colors
  children: JSX.Element
}

export type RoundedBadgeComponent = {
  $backgroundColor: Colors
}
